{% extends 'base.html.twig' %}

{% block title %}Google Drive{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1>Connexion Google Drive</h1>

        <div class="alert alert-success">
            ✅ Connexion réussie à Google Drive
        </div>

        <p><strong>Access Token :</strong> <code>{{ token.access_token }}</code></p>
        <p><strong>Expire dans :</strong> <code>{{ token.expires_in }} secondes</code></p>
        <p><strong>Scope :</strong> <code>{{ token.scope }}</code></p>

        <hr>

        <h2>Actions disponibles</h2>
        <ul>
            <li><a href="{{ path('drive_list_files') }}">📂 Lister les fichiers</a></li>
            <li><a href="{{ path('drive_upload_file') }}">⬆️ Uploader un fichier</a></li>
            <li><a href="{{ path('drive_disconnect') }}">❌ Déconnexion</a></li>
        </ul>
    </div>
{% endblock %}
