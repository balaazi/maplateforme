{% extends 'base.html.twig' %}

{% block title %}Tableau de bord - EventHub{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            /* Couleurs principales */
            --primary-gradient: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            --secondary-gradient: linear-gradient(135deg, #f093fb 0%, #f5576c 100%);
            --success-gradient: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%);
            --warning-gradient: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%);
            
            /* Couleurs de thème clair */
            --bg-primary: #ffffff;
            --bg-secondary: #f8fafc;
            --text-primary: #1a202c;
            --text-secondary: #4a5568;
            --card-bg: rgba(255, 255, 255, 0.9);
            --border-color: rgba(226, 232, 240, 0.8);
            --shadow: 0 8px 32px rgba(0, 0, 0, 0.1);
            --glass-border: rgba(255, 255, 255, 0.2);
        }

        [data-theme="dark"] {
            /* Couleurs de thème sombre */
            --bg-primary: #0f1419;
            --bg-secondary: #1a1f2e;
            --text-primary: #e2e8f0;
            --text-secondary: #a0aec0;
            --card-bg: rgba(26, 31, 46, 0.9);
            --border-color: rgba(45, 55, 72, 0.8);
            --shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            --glass-border: rgba(255, 255, 255, 0.1);
        }

        body {
            background: var(--primary-gradient);
            min-height: 100vh;
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
            color: var(--text-primary);
            transition: all 0.3s ease;
        }

        .dashboard-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 20px;
        }

        /* Header avec thème toggle */
        .dashboard-header {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 20px 30px;
            margin-bottom: 30px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: var(--shadow);
            animation: slideDown 0.6s ease-out;
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .logo {
            font-size: 2rem;
            font-weight: 800;
            background: var(--secondary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .welcome-section h1 {
            font-size: 1.5rem;
            font-weight: 700;
            margin: 0;
            color: var(--text-primary);
        }

        .welcome-section p {
            margin: 5px 0 0 0;
            color: var(--text-secondary);
            font-size: 0.95rem;
        }

        .header-right {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .date-display {
            background: var(--success-gradient);
            color: white;
            padding: 10px 20px;
            border-radius: 15px;
            font-weight: 600;
            font-size: 0.9rem;
        }

        .theme-toggle {
            background: var(--card-bg);
            border: 2px solid var(--glass-border);
            border-radius: 50px;
            padding: 8px;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            width: 60px;
            height: 30px;
        }

        .theme-toggle:hover {
            transform: scale(1.05);
            box-shadow: var(--shadow);
        }

        .theme-toggle i {
            font-size: 1.2rem;
            color: var(--text-primary);
            transition: all 0.3s ease;
        }

        /* Navigation */
        .nav-links {
            display: flex;
            gap: 20px;
        }

        .nav-link {
            color: var(--text-secondary);
            text-decoration: none;
            padding: 8px 16px;
            border-radius: 10px;
            transition: all 0.3s ease;
            font-weight: 500;
        }

        .nav-link:hover {
            color: var(--text-primary);
            background: var(--bg-secondary);
            transform: translateY(-2px);
        }

        /* Section des statistiques avec graphiques */
        .stats-section {
            margin-bottom: 40px;
            animation: fadeInUp 0.8s ease-out;
        }

        .section-title {
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 25px;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            margin-bottom: 30px;
        }

        .stat-card {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 25px;
            text-align: center;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--secondary-gradient);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .stat-card:hover::before {
            transform: scaleX(1);
        }

        .stat-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .stat-icon {
            width: 70px;
            height: 70px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 1.8rem;
            color: white;
            position: relative;
        }

        .stat-icon::after {
            content: '';
            position: absolute;
            inset: 0;
            border-radius: 50%;
            padding: 3px;
            background: var(--secondary-gradient);
            mask: linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0);
            mask-composite: exclude;
        }

        .stat-icon.created { background: var(--success-gradient); }
        .stat-icon.participants { background: var(--warning-gradient); }
        .stat-icon.upcoming { background: var(--secondary-gradient); }
        .stat-icon.participation { background: var(--primary-gradient); }

        .stat-number {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 8px;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .stat-label {
            font-size: 1rem;
            font-weight: 600;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        /* Graphiques interactifs */
        .charts-section {
            margin-bottom: 40px;
            animation: fadeInUp 1s ease-out;
        }

        .charts-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(400px, 1fr));
            gap: 25px;
        }

        .chart-card {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
        }

        .chart-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
        }

        .chart-title {
            font-size: 1.3rem;
            font-weight: 700;
            margin-bottom: 20px;
            color: var(--text-primary);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .chart-container {
            position: relative;
            height: 300px;
        }

        /* NOUVEAU TABLEAU DE BORD ORGANISATEUR */
        .organizer-dashboard {
            margin-bottom: 40px;
        }

        .organizer-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            border-radius: 25px;
            padding: 30px;
            margin-bottom: 30px;
            color: white;
            display: flex;
            justify-content: space-between;
            align-items: center;
            box-shadow: 0 15px 35px rgba(102, 126, 234, 0.3);
        }

        .organizer-info {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .organizer-avatar {
            width: 80px;
            height: 80px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            backdrop-filter: blur(10px);
        }

        .organizer-details h2 {
            margin: 0 0 10px 0;
            font-size: 1.8rem;
            font-weight: 700;
        }

        .organizer-details p {
            margin: 0;
            opacity: 0.9;
            font-size: 1.1rem;
        }

        .organizer-stats {
            display: flex;
            gap: 30px;
        }

        .quick-stat {
            text-align: center;
            background: rgba(255, 255, 255, 0.1);
            padding: 20px;
            border-radius: 15px;
            backdrop-filter: blur(10px);
        }

        .quick-stat .stat-number {
            display: block;
            font-size: 2rem;
            font-weight: 800;
            margin-bottom: 5px;
        }

        .quick-stat .stat-label {
            font-size: 0.9rem;
            opacity: 0.8;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .advanced-stats-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 25px;
            margin-bottom: 40px;
        }

        .stat-card-modern {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
            position: relative;
            overflow: hidden;
        }

        .stat-card-modern::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            height: 4px;
            background: var(--secondary-gradient);
            transform: scaleX(0);
            transition: transform 0.3s ease;
        }

        .stat-card-modern:hover::before {
            transform: scaleX(1);
        }

        .stat-card-modern:hover {
            transform: translateY(-8px);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.15);
        }

        .stat-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
        }

        .stat-icon-modern {
            width: 60px;
            height: 60px;
            border-radius: 15px;
            background: var(--primary-gradient);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: white;
        }

        .stat-trend {
            display: flex;
            align-items: center;
            gap: 5px;
            padding: 8px 12px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .stat-trend.positive {
            background: rgba(34, 197, 94, 0.1);
            color: #22c55e;
        }

        .stat-trend.negative {
            background: rgba(239, 68, 68, 0.1);
            color: #ef4444;
        }

        .stat-trend.neutral {
            background: rgba(156, 163, 175, 0.1);
            color: #9ca3af;
        }

        .stat-content h3 {
            margin: 0 0 15px 0;
            font-size: 1.2rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .stat-number-modern {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 10px;
            background: var(--primary-gradient);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .stat-content p {
            margin: 0;
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        .stat-progress {
            margin-top: 20px;
            height: 6px;
            background: var(--bg-secondary);
            border-radius: 3px;
            overflow: hidden;
        }

        .progress-bar {
            height: 100%;
            background: var(--success-gradient);
            border-radius: 3px;
            transition: width 1s ease;
        }

        .priority-events-section {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 25px;
            padding: 30px;
            box-shadow: var(--shadow);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }

        .section-header h3 {
            margin: 0;
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--text-primary);
        }

        .view-all-btn {
            background: var(--primary-gradient);
            color: white;
            text-decoration: none;
            padding: 10px 20px;
            border-radius: 25px;
            font-weight: 600;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .view-all-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(102, 126, 234, 0.4);
            color: white;
        }

        .priority-events-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 25px;
        }

        .priority-event-card {
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
            border-radius: 20px;
            padding: 25px;
            transition: all 0.3s ease;
            position: relative;
        }

        .priority-event-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.1);
            border-color: var(--primary-gradient);
        }

        .event-status {
            position: absolute;
            top: 20px;
            right: 20px;
            padding: 6px 12px;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .event-status.upcoming {
            background: rgba(34, 197, 94, 0.1);
            color: #22c55e;
        }

        .event-status.past {
            background: rgba(156, 163, 175, 0.1);
            color: #9ca3af;
        }

        .event-content h4 {
            margin: 0 0 15px 0;
            font-size: 1.3rem;
            font-weight: 700;
            color: var(--text-primary);
        }

        .event-category, .event-location {
            margin: 0 0 10px 0;
            color: var(--text-secondary);
            font-size: 0.9rem;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .event-meta {
            display: flex;
            gap: 20px;
            margin: 20px 0;
            flex-wrap: wrap;
        }

        .event-date, .event-participants {
            color: var(--text-secondary);
            font-size: 0.85rem;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .event-actions {
            display: flex;
            gap: 15px;
            margin-top: 20px;
        }

        .action-btn {
            padding: 10px 20px;
            border-radius: 15px;
            text-decoration: none;
            font-weight: 600;
            font-size: 0.9rem;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .action-btn.primary {
            background: var(--success-gradient);
            color: white;
        }

        .action-btn.secondary {
            background: var(--bg-primary);
            color: var(--text-primary);
            border: 1px solid var(--border-color);
        }

        .action-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.15);
        }

        .no-events-placeholder {
            text-align: center;
            padding: 60px 20px;
        }

        .placeholder-icon {
            width: 100px;
            height: 100px;
            background: var(--bg-secondary);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 2.5rem;
            color: var(--text-secondary);
        }

        .no-events-placeholder h4 {
            margin: 0 0 10px 0;
            font-size: 1.5rem;
            color: var(--text-primary);
        }

        .no-events-placeholder p {
            margin: 0 0 25px 0;
            color: var(--text-secondary);
        }

        .create-event-btn {
            background: var(--success-gradient);
            color: white;
            text-decoration: none;
            padding: 15px 30px;
            border-radius: 25px;
            font-weight: 600;
            display: inline-flex;
            align-items: center;
            gap: 10px;
            transition: all 0.3s ease;
        }

        .create-event-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(34, 197, 94, 0.4);
            color: white;
        }

        /* Actions rapides */
        .actions-section {
            margin-bottom: 40px;
            animation: fadeInUp 1.2s ease-out;
        }

        .actions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 20px;
        }

        .action-card {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 18px;
            padding: 25px;
            text-align: center;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
            cursor: pointer;
            position: relative;
            overflow: hidden;
        }

        .action-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.1), transparent);
            transition: left 0.5s ease;
        }

        .action-card:hover::before {
            left: 100%;
        }

        .action-card:hover {
            transform: translateY(-8px) scale(1.02);
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
        }

        .action-icon {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0 auto 20px;
            font-size: 1.5rem;
            color: white;
        }

        .action-icon.create { background: var(--success-gradient); }
        .action-icon.events { background: var(--warning-gradient); }
        .action-icon.calendar { background: var(--secondary-gradient); }
        .action-icon.stats { background: var(--primary-gradient); }

        .action-title {
            font-size: 1.2rem;
            font-weight: 700;
            margin-bottom: 10px;
            color: var(--text-primary);
        }

        .action-description {
            color: var(--text-secondary);
            font-size: 0.9rem;
            line-height: 1.5;
        }

        /* Contenu principal avec filtres */
        .main-content {
            display: grid;
            grid-template-columns: 2fr 1fr;
            gap: 30px;
            animation: fadeInUp 1.4s ease-out;
        }

        .activity-card {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--shadow);
            margin-bottom: 25px;
            transition: all 0.3s ease;
        }

        .activity-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
        }

        /* Filtres pour l'activité récente */
        .filters-section {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        .filter-btn {
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
            color: var(--text-secondary);
            padding: 8px 16px;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .filter-btn.active,
        .filter-btn:hover {
            background: var(--primary-gradient);
            color: white;
            border-color: transparent;
            transform: translateY(-2px);
        }

        .activity-item {
            display: flex;
            align-items: center;
            gap: 15px;
            padding: 20px 0;
            border-bottom: 1px solid var(--border-color);
            transition: all 0.3s ease;
        }

        .activity-item:last-child {
            border-bottom: none;
        }

        .activity-item:hover {
            background: var(--bg-secondary);
            margin: 0 -25px;
            padding: 20px 25px;
            border-radius: 15px;
        }

        .activity-icon {
            width: 50px;
            height: 50px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.2rem;
            color: white;
            flex-shrink: 0;
        }

        .activity-icon.recent { background: var(--success-gradient); }
        .activity-icon.info { background: var(--warning-gradient); }
        .activity-icon.warning { background: var(--secondary-gradient); }

        .activity-content {
            flex: 1;
        }

        .activity-content h5 {
            margin: 0 0 5px 0;
            font-size: 1rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .activity-content p {
            margin: 0;
            color: var(--text-secondary);
            font-size: 0.9rem;
            line-height: 1.4;
        }

        /* Filtres pour l'activité récente */
        .filters-section {
            display: flex;
            gap: 15px;
            margin-bottom: 20px;
            flex-wrap: wrap;
        }

        .filter-btn {
            background: var(--bg-secondary);
            border: 1px solid var(--border-color);
            color: var(--text-secondary);
            padding: 8px 16px;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
            font-size: 0.9rem;
            font-weight: 500;
        }

        .filter-btn.active,
        .filter-btn:hover {
            background: var(--primary-gradient);
            color: white;
            border-color: transparent;
            transform: translateY(-2px);
        }

        .activity-date {
            font-size: 0.85rem;
            color: var(--text-secondary);
            font-weight: 500;
            text-align: right;
            min-width: 80px;
        }

        /* Widgets personnalisables */
        .widgets-section {
            animation: fadeInUp 1.6s ease-out;
        }

        .widget-card {
            background: var(--card-bg);
            backdrop-filter: blur(20px);
            border: 1px solid var(--glass-border);
            border-radius: 18px;
            padding: 20px;
            margin-bottom: 20px;
            box-shadow: var(--shadow);
            transition: all 0.3s ease;
            cursor: move;
        }

        .widget-card:hover {
            transform: translateY(-3px);
            box-shadow: 0 15px 35px rgba(0, 0, 0, 0.15);
        }

        .widget-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 15px;
        }

        .widget-title {
            font-size: 1.1rem;
            font-weight: 600;
            color: var(--text-primary);
        }

        .widget-actions {
            display: flex;
            gap: 8px;
        }

        .widget-btn {
            background: none;
            border: none;
            color: var(--text-secondary);
            cursor: pointer;
            padding: 4px;
            border-radius: 4px;
            transition: all 0.2s ease;
        }

        .widget-btn:hover {
            background: var(--bg-secondary);
            color: var(--text-primary);
        }

        /* Animations CSS */
        @keyframes slideDown {
            from {
                opacity: 0;
                transform: translateY(-30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes fadeInUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }


        @keyframes fadeOut {
            from {
                opacity: 1;
                transform: translateY(0);
            }
            to {
                opacity: 0;
                transform: translateY(-20px);
            }
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .pulse {
            animation: pulse 2s infinite;
        }

        /* Responsive design */
        @media (max-width: 1200px) {
            .main-content {
                grid-template-columns: 1fr;
            }
            
            .charts-grid {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 768px) {
            .dashboard-container {
                padding: 15px;
            }
            
            .dashboard-header {
                flex-direction: column;
                gap: 20px;
                text-align: center;
            }
            
            .stats-grid {
                grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
                gap: 20px;
            }
            
            .actions-grid {
                grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            }
            
            .filters-section {
                justify-content: center;
            }
        }

        /* Notifications en temps réel */
        .notification-badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background: #ef4444;
            color: white;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.7rem;
            font-weight: 600;
            animation: pulse 2s infinite;
        }

        /* Thème toggle animation */
        .theme-toggle[data-theme="dark"] i.fa-sun {
            transform: rotate(180deg);
        }

        .theme-toggle[data-theme="light"] i.fa-moon {
            transform: rotate(180deg);
        }
    </style>
{% endblock %}

{% block body %}
<div class="dashboard-container">
    <!-- Header avec thème toggle -->
    <header class="dashboard-header">
        <div class="header-left">
            <div class="logo">🚀 EventHub</div>
            <div class="welcome-section">
                <h1>Bonjour {{ user.prenom }} {{ user.nom }}!</h1>
                <p>Gérez vos événements et suivez votre activité en temps réel</p>
                    </div>
                </div>
        
        <div class="header-right">
            <div class="date-display">
                        {{ "now"|date("l d M Y") }}
                    </div>
            
            <button class="theme-toggle" id="themeToggle" data-theme="light">
                <i class="fas fa-sun"></i>
            </button>
            
            <nav class="nav-links">
                <a href="{{ path('home') }}" class="nav-link">Accueil</a>
                <a href="{{ path('calendar_index') }}" class="nav-link">Agenda</a>
                <a href="#" class="nav-link">Notifications <span class="notification-badge">3</span></a>
                <a href="{{ path('user_profile') }}" class="nav-link">Mon Compte</a>
            </nav>
                </div>
    </header>

    <!-- Section des statistiques avec graphiques -->
        <div class="stats-section">
            <h2 class="section-title">
            📊 Tableau de bord
            </h2>
            <div class="stats-grid">
            {% if is_organisateur %}
                <div class="stat-card">
                <div class="stat-icon created">
                    <i class="fas fa-calendar-plus"></i>
                </div>
                <div class="stat-number">{{ organisateur_stats.total_my_events ?? 0 }}</div>
                <div class="stat-label">Créés</div>
            </div>

                <div class="stat-card">
                <div class="stat-icon participants">
                    <i class="fas fa-users"></i>
                </div>
                <div class="stat-number">{{ organisateur_stats.total_participants ?? 0 }}</div>
                <div class="stat-label">Participants</div>
            </div>

            <div class="stat-card">
                <div class="stat-icon upcoming">
                    <i class="fas fa-clock"></i>
        </div>
                <div class="stat-number">{{ organisateur_stats.upcoming_events ?? global_stats.upcoming_events ?? 0 }}</div>
                <div class="stat-label">À venir</div>
            </div>

                <div class="stat-card">
                <div class="stat-icon participation">
                    <i class="fas fa-chart-line"></i>
                </div>
                <div class="stat-number">{{ global_stats.participation_rate ?? 0 }}</div>
                <div class="stat-label">Participation %</div>
            </div>

            <!-- Lien vers les statistiques avancées -->
            <div class="stat-card clickable" onclick="window.location.href='{{ path('advanced_statistics') }}'">
                <div class="stat-icon advanced">
                    <i class="fas fa-chart-bar"></i>
                </div>
                <div class="stat-label">Statistiques avancées</div>
                <div class="stat-description">Analyse détaillée</div>
            </div>
            {% else %}
            <div class="stat-card">
                <div class="stat-icon created">
                    <i class="fas fa-calendar"></i>
                </div>
                <div class="stat-number">{{ global_stats.total_events ?? 0 }}</div>
                <div class="stat-label">Total événements</div>
            </div>

            <div class="stat-card">
                <div class="stat-icon participants">
                    <i class="fas fa-users"></i>
                </div>
                <div class="stat-number">{{ global_stats.total_participants ?? 0 }}</div>
                <div class="stat-label">Participants</div>
            </div>

            <div class="stat-card">
                <div class="stat-icon upcoming">
                    <i class="fas fa-clock"></i>
                </div>
                <div class="stat-number">{{ global_stats.upcoming_events ?? 0 }}</div>
                <div class="stat-label">À venir</div>
            </div>

            <div class="stat-card">
                <div class="stat-icon participation">
                    <i class="fas fa-chart-line"></i>
                </div>
                <div class="stat-number">{{ global_stats.participation_rate ?? 0 }}</div>
                <div class="stat-label">Participation %</div>
        </div>
        {% endif %}

                <!-- Activités simulées pour démonstration -->
                <div class="activity-item" data-category="participants">
                    <div class="activity-icon recent">
                        <i class="fas fa-user-plus"></i>
                    </div>
                    <div class="activity-content">
                        <h5>3 nouvelles inscriptions</h5>
                        <p>Formation Angular - Nouvelles inscriptions reçues</p>
                    </div>
                    <div class="activity-date">Il y a 2h</div>
                </div>

                <div class="activity-item" data-category="notifications">
                    <div class="activity-icon recent">
                        <i class="fas fa-paper-plane"></i>
                    </div>
                    <div class="activity-content">
                        <h5>Rappel envoyé</h5>
                        <p>Notification de rappel envoyée pour la réunion demain</p>
                    </div>
                    <div class="activity-date">Il y a 5h</div>
                </div>
            </div>
        </div>

    <!-- Actions rapides -->
        <div class="actions-section">
            <h2 class="section-title">
            ⚡ Actions rapides
            </h2>
            <div class="actions-grid">
                <a href="{{ path('event_create') }}" class="action-card">
                <div class="action-icon create">
                        <i class="fas fa-plus"></i>
                    </div>
                <h3 class="action-title">Créer un événement</h3>
                <p class="action-description">Organisez un nouvel événement en quelques clics</p>
                </a>

            <a href="{{ path('event_list') }}" class="action-card">
                <div class="action-icon events">
                        <i class="fas fa-list"></i>
                    </div>
                <h3 class="action-title">Mes événements</h3>
                <p class="action-description">Consultez et gérez tous vos événements</p>
                </a>

                <a href="{{ path('calendar_index') }}" class="action-card">
                <div class="action-icon calendar">
                        <i class="fas fa-calendar"></i>
                    </div>
                <h3 class="action-title">Calendrier</h3>
                <p class="action-description">Vue calendrier de tous vos événements</p>
            </a>

            <a href="{{ path('stats_index') }}" class="action-card">
                <div class="action-icon stats">
                    <i class="fas fa-chart-bar"></i>
                    </div>
                <h3 class="action-title">Statistiques</h3>
                <p class="action-description">Analysez vos performances détaillées</p>
                </a>
            </div>
        </div>

    <!-- Graphiques interactifs -->
    <div class="charts-section">
            <h2 class="section-title">
            📈 Analyses détaillées
            </h2>
        <div class="charts-grid">
            <div class="chart-card">
                <h3 class="chart-title">
                    <i class="fas fa-chart-pie"></i>
                    Répartition des événements
                </h3>
                <div class="chart-container">
                    <canvas id="eventsChart"></canvas>
                        </div>
                        </div>
            
            <div class="chart-card">
                <h3 class="chart-title">
                    <i class="fas fa-chart-line"></i>
                    Évolution mensuelle
                </h3>
                <div class="chart-container">
                    <canvas id="monthlyChart"></canvas>
                    </div>
            </div>
        </div>
    </div>

    <!-- Contenu principal avec filtres -->
    <div class="main-content">
        <!-- Activité récente avec filtres -->
        <div class="activity-card">
            <h3 class="section-title">
                🕐 Activité récente
            </h3>
            
            <!-- Filtres améliorés -->
            <div class="filters-section">
                <button class="filter-btn active" data-filter="all">
                    <i class="fas fa-list"></i> Tout
                </button>
                <button class="filter-btn" data-filter="events">
                    <i class="fas fa-calendar"></i> Événements
                </button>
                <button class="filter-btn" data-filter="participants">
                    <i class="fas fa-users"></i> Participants
                </button>
                <button class="filter-btn" data-filter="notifications">
                    <i class="fas fa-bell"></i> Notifications
                </button>
                <button class="filter-btn" data-filter="documents">
                    <i class="fas fa-file-alt"></i> Documents
                </button>
            </div>

            <div class="activity-list">
                {% if is_organisateur and organisateur_stats.my_events is defined and organisateur_stats.my_events|length > 0 %}
                    {% for event in organisateur_stats.my_events|slice(0, 3) %}
                    <div class="activity-item" data-category="events">
                        <div class="activity-icon recent">
                            {% if event.category == 'Formation' %}
                                <i class="fas fa-graduation-cap"></i>
                            {% elseif event.category == 'Réunion' %}
                                <i class="fas fa-handshake"></i>
                            {% elseif event.category == 'Séminaire' %}
                                <i class="fas fa-chalkboard-teacher"></i>
                        {% else %}
                                <i class="fas fa-calendar"></i>
                            {% endif %}
                        </div>
                        <div class="activity-content">
                            <h5>{{ event.title }}</h5>
                            <p>
                                {% if event.salle %}
                                    📍 {{ event.salle.nom }}
                                {% elseif event.lieu %}
                                    📍 {{ event.lieu }}
                                {% else %}
                                    📍 Lieu non défini
                                {% endif %}
                                • {{ event.category }}
                            </p>
                        </div>
                        <div class="activity-date">
                            {% if event.dateHeure > date() %}
                                <span style="color: #38ef7d;">{{ event.dateHeure|date('d/m à H:i') }}</span>
                            {% else %}
                                <span style="color: #f093fb;">{{ event.dateHeure|date('d/m à H:i') }}</span>
                        {% endif %}
                    </div>
                </div>
                {% endfor %}
                {% else %}
                    <div class="activity-item" data-category="events">
                        <div class="activity-icon recent">
                            <i class="fas fa-rocket"></i>
                        </div>
                        <div class="activity-content">
                            <h5>Commencez votre aventure !</h5>
                            <p>Créez votre premier événement dès maintenant</p>
                        </div>
                        <div class="activity-date">
                            <a href="{{ path('event_create') }}" style="color: #38ef7d; text-decoration: none;">
                                → Créer
                            </a>
            </div>
        </div>
        {% endif %}

                <!-- Activités dynamiques et réalistes -->
                <div class="activity-item" data-category="participants">
                    <div class="activity-icon recent">
                        <i class="fas fa-user-plus"></i>
                        </div>
                    <div class="activity-content">
                        <h5>5 nouvelles inscriptions</h5>
                        <p>Formation Angular - Nouvelles inscriptions reçues pour la session de demain</p>
                        </div>
                    <div class="activity-date">
                        <span style="color: #38ef7d;">Il y a 1h</span>
                    </div>
                    </div>

                <div class="activity-item" data-category="participants">
                    <div class="activity-icon recent">
                        <i class="fas fa-user-check"></i>
                </div>
                    <div class="activity-content">
                        <h5>Présence confirmée</h5>
                        <p>12 participants ont confirmé leur présence pour la réunion d'équipe</p>
            </div>
                    <div class="activity-date">
                        <span style="color: #4facfe;">Il y a 3h</span>
        </div>
    </div>

                <div class="activity-item" data-category="notifications">
                    <div class="activity-icon recent">
                        <i class="fas fa-paper-plane"></i>
</div>
                    <div class="activity-content">
                        <h5>Rappels automatiques envoyés</h5>
                        <p>15 notifications de rappel envoyées pour les événements de cette semaine</p>
                    </div>
                    <div class="activity-date">
                        <span style="color: #f093fb;">Il y a 6h</span>
                    </div>
                </div>

                <div class="activity-item" data-category="notifications">
                    <div class="activity-icon recent">
                        <i class="fas fa-bell"></i>
                    </div>
                    <div class="activity-content">
                        <h5>Nouvelle notification</h5>
                        <p>Rappel : Réunion de planification demain à 14h00</p>
                    </div>
                    <div class="activity-date">
                        <span style="color: #ff6b6b;">Il y a 8h</span>
                    </div>
                </div>

                <div class="activity-item" data-category="documents">
                    <div class="activity-icon recent">
                        <i class="fas fa-file-upload"></i>
                    </div>
                    <div class="activity-content">
                        <h5>Document partagé</h5>
                        <p>Agenda de la réunion partagé avec tous les participants</p>
                    </div>
                    <div class="activity-date">
                        <span style="color: #4ecdc4;">Hier à 16h30</span>
                    </div>
                </div>

                <div class="activity-item" data-category="events">
                    <div class="activity-icon recent">
                        <i class="fas fa-calendar-check"></i>
                    </div>
                    <div class="activity-content">
                        <h5>Événement planifié</h5>
                        <p>Nouvelle formation "React Avancé" planifiée pour la semaine prochaine</p>
                    </div>
                    <div class="activity-date">
                        <span style="color: #45b7d1;">Il y a 1 jour</span>
                    </div>
                </div>

                <div class="activity-item" data-category="documents">
                    <div class="activity-icon recent">
                        <i class="fas fa-chart-bar"></i>
                    </div>
                    <div class="activity-content">
                        <h5>Rapport généré</h5>
                        <p>Rapport mensuel des événements généré et envoyé par email</p>
                    </div>
                    <div class="activity-date">
                        <span style="color: #96ceb4;">Il y a 2 jours</span>
                    </div>
                </div>
            </div>
        </div>

        <!-- Widgets personnalisables -->
        <div class="widgets-section">
            <!-- Informations utiles -->
            <div class="widget-card" data-widget="info">
                <div class="widget-header">
                    <h4 class="widget-title">💡 Conseils & Nouveautés</h4>
                    <div class="widget-actions">
                        <button class="widget-btn" onclick="refreshWidget('info')">
                            <i class="fas fa-sync-alt"></i>
                        </button>
                        <button class="widget-btn" onclick="minimizeWidget('info')">
                            <i class="fas fa-minus"></i>
                        </button>
                    </div>
                </div>
                
                {% set tips = [
                    { icon: 'lightbulb', title: 'Conseil du jour', content: 'Envoyez vos invitations 48h avant pour un meilleur taux de participation.' },
                    { icon: 'clock', title: 'Planification', content: 'Prévoyez 15 minutes de marge entre vos événements pour la transition.' },
                    { icon: 'users', title: 'Engagement', content: 'Les formations interactives ont 30% plus de participation que les autres.' },
                    { icon: 'chart-line', title: 'Statistiques', content: 'Votre taux de participation moyen est de ' ~ (global_stats.participation_rate ?? 0) ~ '%.' }
                ] %}
                
                {% set news = [
                    { icon: 'star', title: 'Nouveautés', content: 'Système de liste de présence disponible pour toutes les formations.' },
                    { icon: 'calendar-check', title: 'Amélioration', content: 'Export PDF des procès-verbaux maintenant disponible.' },
                    { icon: 'bell', title: 'Rappels', content: 'Notifications automatiques activées pour vos événements.' }
                ] %}

                {% set currentTip = tips[random(tips|length - 1)] %}
                {% set currentNews = news[random(news|length - 1)] %}

                <div class="activity-item">
                    <div class="activity-icon info">
                        <i class="fas fa-{{ currentTip.icon }}"></i>
                    </div>
                    <div class="activity-content">
                        <h5>{{ currentTip.title }}</h5>
                        <p>{{ currentTip.content }}</p>
                    </div>
                </div>

                <div class="activity-item">
                    <div class="activity-icon info">
                        <i class="fas fa-{{ currentNews.icon }}"></i>
                    </div>
                    <div class="activity-content">
                        <h5>{{ currentNews.title }}</h5>
                        <p>{{ currentNews.content }}</p>
                    </div>
                </div>

                <div class="activity-item">
                    <div class="activity-icon info">
                        <i class="fas fa-question-circle"></i>
                    </div>
                    <div class="activity-content">
                        <h5>Support</h5>
                        <p>Besoin d'aide ? Le bouton "Liste de présence" est maintenant accessible avant le jour J !</p>
                    </div>
                    <div class="activity-date">
                        <span style="color: #4facfe;">Nouveau</span>
                    </div>
                </div>
            </div>

            <!-- Widget météo (simulé) -->
            <div class="widget-card" data-widget="weather">
                <div class="widget-header">
                    <h4 class="widget-title">🌤️ Météo locale</h4>
                    <div class="widget-actions">
                        <button class="widget-btn" onclick="refreshWidget('weather')">
                            <i class="fas fa-sync-alt"></i>
                        </button>
                        <button class="widget-btn" onclick="minimizeWidget('weather')">
                            <i class="fas fa-minus"></i>
                        </button>
                    </div>
                </div>
                <div class="weather-content">
                    <div style="text-align: center; padding: 20px;">
                        <i class="fas fa-sun" style="font-size: 3rem; color: #f59e0b; margin-bottom: 15px;"></i>
                        <h3 style="margin: 0; color: var(--text-primary);">22°C</h3>
                        <p style="margin: 5px 0; color: var(--text-secondary);">Ensoleillé</p>
                        <p style="margin: 0; font-size: 0.9rem; color: var(--text-secondary);">Parfait pour vos événements en extérieur !</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Scripts pour les graphiques et interactions -->
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
// Gestion du thème
const themeToggle = document.getElementById('themeToggle');
const body = document.body;

// Charger le thème sauvegardé
const savedTheme = localStorage.getItem('theme') || 'light';
setTheme(savedTheme);

themeToggle.addEventListener('click', () => {
    const currentTheme = body.getAttribute('data-theme');
    const newTheme = currentTheme === 'light' ? 'dark' : 'light';
    setTheme(newTheme);
});

function setTheme(theme) {
    body.setAttribute('data-theme', theme);
    themeToggle.setAttribute('data-theme', theme);
    themeToggle.innerHTML = theme === 'light' ? '<i class="fas fa-moon"></i>' : '<i class="fas fa-sun"></i>';
    localStorage.setItem('theme', theme);
}

// Filtres pour l'activité récente
const filterBtns = document.querySelectorAll('.filter-btn');
const activityItems = document.querySelectorAll('.activity-item');

filterBtns.forEach(btn => {
    btn.addEventListener('click', () => {
        // Retirer la classe active de tous les boutons
        filterBtns.forEach(b => b.classList.remove('active'));
        // Ajouter la classe active au bouton cliqué
        btn.classList.add('active');
        
        const filter = btn.getAttribute('data-filter');
        
        // Animation de transition pour les filtres
        activityItems.forEach(item => {
            if (filter === 'all' || item.getAttribute('data-category') === filter) {
                item.style.display = 'flex';
                item.style.animation = 'fadeInUp 0.3s ease-out';
                item.style.opacity = '1';
                item.style.transform = 'translateY(0)';
            } else {
                item.style.animation = 'fadeOut 0.2s ease-out';
                setTimeout(() => {
                    item.style.display = 'none';
                }, 200);
            }
        });
        
        // Afficher le nombre d'éléments filtrés
        const visibleItems = document.querySelectorAll(`.activity-item[data-category="${filter}"], .activity-item[data-filter="all"]`);
        console.log(`${visibleItems.length} activités affichées pour le filtre: ${filter}`);
    });
});

// Graphiques Chart.js
document.addEventListener('DOMContentLoaded', function() {
    // Graphique en secteurs pour les événements
    const eventsCtx = document.getElementById('eventsChart');
    if (eventsCtx) {
        new Chart(eventsCtx, {
            type: 'doughnut',
            data: {
                labels: ['Formations', 'Réunions', 'Séminaires', 'Autres'],
                datasets: [{
                    data: [40, 25, 20, 15],
                    backgroundColor: [
                        '#4facfe',
                        '#f093fb',
                        '#43e97b',
                        '#f5576c'
                    ],
                    borderWidth: 0
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        position: 'bottom',
                        labels: {
                            color: getComputedStyle(document.documentElement).getPropertyValue('--text-primary'),
                            padding: 20,
                            usePointStyle: true
                        }
                    }
                }
            }
        });
    }
    
    // Graphique linéaire pour l'évolution mensuelle
    const monthlyCtx = document.getElementById('monthlyChart');
    if (monthlyCtx) {
        new Chart(monthlyCtx, {
            type: 'line',
            data: {
                labels: ['Jan', 'Fév', 'Mar', 'Avr', 'Mai', 'Juin'],
                datasets: [{
                    label: 'Événements créés',
                    data: [12, 19, 15, 25, 22, 30],
                    borderColor: '#4facfe',
                    backgroundColor: 'rgba(79, 172, 254, 0.1)',
                    tension: 0.4,
                    fill: true
                }]
            },
            options: {
                responsive: true,
                maintainAspectRatio: false,
                plugins: {
                    legend: {
                        labels: {
                            color: getComputedStyle(document.documentElement).getPropertyValue('--text-primary')
                        }
                    }
                },
                scales: {
                    y: {
                        beginAtZero: true,
                        grid: {
                            color: getComputedStyle(document.documentElement).getPropertyValue('--border-color')
                        },
                        ticks: {
                            color: getComputedStyle(document.documentElement).getPropertyValue('--text-secondary')
                        }
                    },
                    x: {
                        grid: {
                            color: getComputedStyle(document.documentElement).getPropertyValue('--border-color')
                        },
                        ticks: {
                            color: getComputedStyle(document.documentElement).getPropertyValue('--text-secondary')
                        }
                    }
                }
            }
        });
    }
});

// Fonctions pour les widgets
function refreshWidget(widgetId) {
    const widget = document.querySelector(`[data-widget="${widgetId}"]`);
    widget.style.animation = 'pulse 0.5s ease-in-out';
    setTimeout(() => {
        widget.style.animation = '';
    }, 500);
    
    // Simuler un rafraîchissement
    console.log(`Widget ${widgetId} rafraîchi`);
}

function minimizeWidget(widgetId) {
    const widget = document.querySelector(`[data-widget="${widgetId}"]`);
    const content = widget.querySelector('.widget-content, .weather-content');
    
    if (content.style.display === 'none') {
        content.style.display = 'block';
        widget.querySelector('.fa-minus').className = 'fas fa-minus';
    } else {
        content.style.display = 'none';
        widget.querySelector('.fa-minus').className = 'fas fa-plus';
    }
}

// Notifications en temps réel (simulation)
setInterval(() => {
    const badge = document.querySelector('.notification-badge');
    if (badge) {
        const currentCount = parseInt(badge.textContent);
        if (Math.random() > 0.8) { // 20% de chance
            badge.textContent = currentCount + 1;
            badge.style.animation = 'pulse 0.5s ease-in-out';
            setTimeout(() => {
                badge.style.animation = '';
            }, 500);
        }
    }
}, 10000); // Vérifier toutes les 10 secondes

// Animation d'entrée pour les éléments
const observerOptions = {
    threshold: 0.1,
    rootMargin: '0px 0px -50px 0px'
};

const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
        if (entry.isIntersecting) {
            entry.target.style.opacity = '1';
            entry.target.style.transform = 'translateY(0)';
        }
    });
}, observerOptions);

// Observer tous les éléments avec animation
document.querySelectorAll('.stat-card, .action-card, .activity-card, .chart-card').forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(30px)';
    el.style.transition = 'all 0.6s ease-out';
    observer.observe(el);
});
</script>
{% endblock %} 